# Local backend environment example
# Copy to .env (NOT committed) and fill values

# REQUIRED: Mongo connection string (Atlas or local)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<db>?retryWrites=true&w=majority

# App Port (your local URL uses 5000)
PORT=5000

# JWT secret (any random string)
JWT_SECRET=change_me_to_a_long_random_string

# Super admin credentials (Admin panel login)
SUPER_ADMIN_ID=Rahul@Malik
SUPER_ADMIN_PASSWORD=Rupantramalik@rahul

# Optional: extra CORS origins (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3005
